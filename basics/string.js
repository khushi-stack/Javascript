const name = "khushi"
const repoCount = 90

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);

// remember to use backticks ` which are needed for template literals to work in JavaScript.

const game= new String("Khushi")

console.log(game.toUpperCase())
console.log(game.charAt(2))
console.log(game.indexOf('t'))

const newString = game.substring(0,5)
console.log(newString);


const newStringsss = "    hiteshh    "
console.log(newStringsss);
console.log(newStringsss.trim());
const url = "https://goog%20le.com"

console.log(url.replace('%20' , "-"))
